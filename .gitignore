# butler working state (regenerable scratch — never committed)
.butler/
review/

# stray evidence/probe shots that leak into the repo root (butler evidence belongs in .butler/state/<id>/).
# root-level only — assets/icons/*.png stay tracked.
/*.png
.playwright-mcp/

# misc
node_modules/
.DS_Store
